import { PasswordEditorDirective } from './password-editor.directive';

describe('PasswordEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordEditorDirective();
    expect(directive).toBeTruthy();
  });
});
